{% if pageCount > 1 %}
<div class="pagination">
    {% if first is defined and current != first %}
            <a href="{{ path(route, query|merge({(pageParameterName): first})) }}" title="First Page">&laquo; Première page</a>
    {% endif %}

    {% if previous is defined %}
            <a href="{{ path(route, query|merge({(pageParameterName): previous})) }}">&laquo; Prec.</a>
    {% endif %}

    {% for page in pagesInRange %}
        {% if page != current %}
            <a class="number" href="{{ path(route, query|merge({(pageParameterName): page})) }}">{{ page }}</a>
        {% else %}
            <a class="number current">{{ page }}</a>
        {% endif %}

    {% endfor %}

    {% if next is defined %}
         <a href="{{ path(route, query|merge({(pageParameterName): next})) }}" title="Next Page">Suiv &raquo;</a>
    {% endif %}

    {% if last is defined and current != last %}
         <a href="{{ path(route, query|merge({(pageParameterName): last})) }}" title="Last Page">Dernière page &raquo;</a>
    {% endif %}
</div>

{% endif %}
